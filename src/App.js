import styled from "styled-components";
import { LandingPage } from "./Pages";

const Container = styled.div``;

export const App = () => {
  return (
    <Container>
      <LandingPage />
    </Container>
  );
};
